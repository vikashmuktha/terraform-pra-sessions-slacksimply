# Variables

aws_region = "eu-west-3"
environment = "Dev"  # Actually we are providing in generic variables is "stagging" it will replace it by terraform.tfvars
business_division = "HR" # Actually we are providing in generic variables is "SAP" it will replace it by terraform.tfvars 